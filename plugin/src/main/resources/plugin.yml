name: ${rootProject.name}
version: ${rootProject.version}
api-version: 1.17
main: net.projecttl.pbalance.PBalance

commands:
  pbalance:
    description: This is money command
    usage: /pbalance <option> <target_player> <amount>
    aliases:
      - money