name: ${rootProject.name}
version: ${rootProject.version}
api-version: 1.17
main: net.projecttl.peconomy.PEconomy

commands:
  peconomy:
    description: This is money command
    usage: /peconomy <option> <target_player> <amount>
    aliases:
      - money
  exchange:
    description: This is money command
    usage: /exchange
    aliases:
      - ex