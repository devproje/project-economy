name: ${rootProject.name}
version: ${project.version}
api-version: 1.17
main: net.projecttl.plugin.ProjectEconomy

commands:
  money:
    description: This is money command
    usage: /money <option> <target_player> <amount>
    aliases:
      - money
      - economy